(()=>{const C=window.DEUCAPA_CLOUD_CONFIG,db=C&&window.supabase?window.supabase.createClient(C.url,C.key):null,$=s=>document.querySelector(s),esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));if(!db)return;let campaigns=[],selectedProspect=null;const activeEdition=()=>localStorage.getItem('deuCapaActiveEdition')||window.DEUCAPA_EDITION?.id||'edicao-01';const pkey=()=>`deuCapaParticipants::${activeEdition()}`;function readParticipants(){try{const x=JSON.parse(localStorage.getItem(pkey())||'[]');return Array.isArray(x)?x:[]}catch{return[]}}function saveParticipants(x){localStorage.setItem(pkey(),JSON.stringify(x));if(window.DEUCAPA_SYNC_NOW)window.DEUCAPA_SYNC_NOW()}async function logEvent(prospectId,eventType,note=''){await db.from('prospect_contact_events').insert({prospect_id:prospectId,event_type:eventType,note:note||null})}async function loadCampaigns(){const {data,error}=await db.from('prospect_campaigns').select('*').order('created_at',{ascending:false});if(error){console.error(error);return}campaigns=data||[];renderCampaigns()}function ensure(){const panel=$('[data-panel="prospeccao"]');if(!panel||$('#prospectCRM'))return;const box=document.createElement('div');box.id='prospectCRM';box.className='panel-card';box.innerHTML=`<div class="section-title"><h2>Campanhas & histórico</h2><button id="prospectCRMRefresh" class="text-btn" type="button">Atualizar</button></div><div style="display:grid;grid-template-columns:minmax(0,1fr) minmax(0,1.3fr);gap:16px"><div><label>Nova campanha<input id="campaignName" placeholder="Ex.: Edição Setembro — Lifestyle BH"></label><div class="field-grid"><label>Cidade<input id="campaignCity" placeholder="Belo Horizonte"></label><label>Nicho<input id="campaignCategory" placeholder="Lifestyle"></label></div><button id="createCampaignBtn" class="primary-btn" type="button">Criar campanha</button><div id="campaignList" style="margin-top:12px"></div></div><div><div id="prospectHistoryState" class="prospect-stat">Selecione um candidato pelo histórico para ver os eventos.</div><div id="prospectHistoryList"></div></div></div>`;panel.appendChild(box);$('#createCampaignBtn').onclick=createCampaign;$('#prospectCRMRefresh').onclick=()=>{loadCampaigns();decorateCards()}}async function createCampaign(){const name=$('#campaignName')?.value.trim(),city=$('#campaignCity')?.value.trim()||null,category=$('#campaignCategory')?.value.trim()||null;if(!name)return alert('Digite o nome da campanha.');const {error}=await db.from('prospect_campaigns').insert({name,city,category});if(error)return alert(error.message);$('#campaignName').value='';$('#campaignCity').value='';$('#campaignCategory').value='';loadCampaigns()}function renderCampaigns(){const box=$('#campaignList');if(!box)return;if(!campaigns.length){box.innerHTML='<p class="prospect-empty">Nenhuma campanha criada.</p>';return}box.innerHTML=campaigns.map(c=>`<div class="prospect-insight-row"><div><strong>${esc(c.name)}</strong><small>${esc(c.city||'Todas as cidades')} · ${esc(c.category||'Todos os nichos')} · ${esc(c.status)}</small></div><button type="button" data-campaign-use="${c.id}">Usar</button></div>`).join('');box.querySelectorAll('[data-campaign-use]').forEach(b=>b.onclick=()=>{const c=campaigns.find(x=>x.id===b.dataset.campaignUse);if(!c)return;const city=$('#prospectCity'),cat=$('#prospectCategory');if(city&&c.city)city.value=c.city;if(cat&&c.category)cat.value=c.category;localStorage.setItem('deuCapaProspectCampaign',c.id);b.textContent='Selecionada ✓';setTimeout(()=>b.textContent='Usar',1200)})}async function showHistory(p){selectedProspect=p;const state=$('#prospectHistoryState'),list=$('#prospectHistoryList');if(state)state.innerHTML=`Histórico de <strong>@${esc(p.handle)}</strong>`;const {data,error}=await db.from('prospect_contact_events').select('*').eq('prospect_id',p.id).order('created_at',{ascending:false}).limit(100);if(error){if(list)list.innerHTML='<p class="prospect-empty">Erro ao carregar histórico.</p>';return}const rows=data||[];if(list)list.innerHTML=rows.length?rows.map(e=>`<div class="prospect-insight-row"><div><strong>${esc(e.event_type)}</strong><small>${new Date(e.created_at).toLocaleString('pt-BR')}${e.note?' · '+esc(e.note):''}</small></div></div>`).join(''):'<p class="prospect-empty">Nenhum evento registrado ainda.</p>'}async function convertToParticipant(p,btn){const participants=readParticipants();const exists=participants.find(x=>String(x.social||'').toLowerCase().includes(String(p.handle).toLowerCase())||String(x.id)===String(p.participant_id||''));if(exists){btn.textContent='Já é participante';return}const id=`p-${Date.now()}`,participant={id,name:`@${p.handle}`,role:[p.category,p.city].filter(Boolean).join(' · ')||'Participante',bio:`Perfil convertido da prospecção DEU CAPA${p.notes?' — '+String(p.notes).slice(0,180):''}`,social:`https://www.instagram.com/${p.handle}/`,photo:'',extraPhotos:['','',''],flip3DPhotos:['main']};participants.unshift(participant);saveParticipants(participants);const now=new Date().toISOString();const {error}=await db.from('participant_prospects').update({status:'participante',participant_id:id,converted_at:now,updated_at:now}).eq('id',p.id);if(error)return alert(error.message);await logEvent(p.id,'convertido_participante',`Criado participante ${id}`);btn.textContent='Participante criado ✓';setTimeout(()=>{document.querySelector('[data-tab="participantes"]')?.click()},700)}async function assignCampaign(p,campaignId,btn){const {error}=await db.from('participant_prospects').update({campaign_id:campaignId||null,updated_at:new Date().toISOString()}).eq('id',p.id);if(error)return alert(error.message);await logEvent(p.id,'campanha_atualizada',campaignId?`Campanha ${campaignId}`:'Sem campanha');btn.textContent='Campanha salva ✓';setTimeout(()=>btn.textContent='Salvar campanha',1000)}async function addNote(p){const note=prompt(`Adicionar observação ao histórico de @${p.handle}:`);if(!note)return;await logEvent(p.id,'nota',note);showHistory(p)}function decorateCards(){document.querySelectorAll('.prospect-card').forEach(card=>{if(card.querySelector('.prospect-crm-tools'))return;const id=card.dataset.id,handle=card.querySelector('.prospect-handle')?.textContent?.replace(/^@/,'');if(!id||!handle)return;db.from('participant_prospects').select('*').eq('id',id).maybeSingle().then(({data:p})=>{if(!p)return;const tools=document.createElement('div');tools.className='prospect-crm-tools';tools.style.cssText='margin-top:10px;padding-top:10px;border-top:1px dashed rgba(145,113,56,.28);display:flex;flex-wrap:wrap;gap:7px;align-items:center';const opts=['<option value="">Sem campanha</option>',...campaigns.map(c=>`<option value="${c.id}" ${p.campaign_id===c.id?'selected':''}>${esc(c.name)}</option>`)].join('');tools.innerHTML=`<select data-crm-campaign style="min-width:180px;max-width:100%">${opts}</select><button type="button" data-crm-save>Salvar campanha</button><button type="button" data-crm-history>Histórico</button><button type="button" data-crm-note>+ Nota</button>${p.status==='interessado'||p.status==='respondeu'||p.status==='participante'?`<button type="button" class="primary-btn" data-crm-convert>${p.participant_id?'Participante criado':'Criar participante'}</button>`:''}`;card.appendChild(tools);const sel=tools.querySelector('[data-crm-campaign]');tools.querySelector('[data-crm-save]').onclick=e=>assignCampaign(p,sel.value,e.currentTarget);tools.querySelector('[data-crm-history]').onclick=()=>showHistory(p);tools.querySelector('[data-crm-note]').onclick=()=>addNote(p);const convert=tools.querySelector('[data-crm-convert]');if(convert)convert.onclick=e=>convertToParticipant(p,e.currentTarget)})})}function observeStageEvents(){document.addEventListener('click',e=>{const btn=e.target.closest('.prospect-card [data-act]');if(!btn)return;const card=btn.closest('.prospect-card'),id=card?.dataset.id;if(!id)return;const act=btn.dataset.act,map={approve:'aprovado',contact:'contatado',reply:'respondeu',interest:'interessado',convert:'participante',discard:'descartado'};if(!map[act])return;setTimeout(()=>logEvent(id,`status_${map[act]}`),350)},true)}function boot(){ensure();loadCampaigns().then(()=>setTimeout(decorateCards,400));observeStageEvents();new MutationObserver(()=>decorateCards()).observe(document.body,{childList:true,subtree:true})}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(boot,1800));else setTimeout(boot,1800)})();