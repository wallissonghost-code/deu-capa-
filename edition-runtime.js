(()=>{const META='deuCapaEditions',ACTIVE='deuCapaActiveEdition',KEYS=new Set(['deuCapaConfig','deuCapaParticipants','deuCapaSponsors']);const g=Storage.prototype.getItem,s=Storage.prototype.setItem,r=Storage.prototype.removeItem;const rawGet=k=>g.call(localStorage,k),rawSet=(k,v)=>s.call(localStorage,k,v),rawRemove=k=>r.call(localStorage,k);let editions;try{editions=JSON.parse(rawGet(META)||'null')}catch{}if(!Array.isArray(editions)||!editions.length){editions=[{id:'edicao-01',name:'Edição 01',createdAt:new Date().toISOString()}];rawSet(META,JSON.stringify(editions));const id=editions[0].id;KEYS.forEach(k=>{const old=rawGet(k);if(old!=null&&rawGet(`${k}::${id}`)==null)rawSet(`${k}::${id}`,old)})}const requested=new URLSearchParams(location.search).get('edition');let active=requested&&editions.some(e=>e.id===requested)?requested:rawGet(ACTIVE);if(!active||!editions.some(e=>e.id===active))active=editions[0].id;rawSet(ACTIVE,active);const scoped=k=>KEYS.has(String(k))?`${k}::${active}`:k;Storage.prototype.getItem=function(k){return g.call(this,scoped(k))};Storage.prototype.setItem=function(k,v){return s.call(this,scoped(k),v)};Storage.prototype.removeItem=function(k){return r.call(this,scoped(k))};window.DEUCAPA_EDITION={id:active,list:editions,rawGet,rawSet,rawRemove,metaKey:META,activeKey:ACTIVE,keys:[...KEYS]};})();