(()=>{const C=window.DEUCAPA_CLOUD_CONFIG,box=document.getElementById('editionArchive');if(!C||!box)return;const H={apikey:C.key,Authorization:`Bearer ${C.key}`},esc=s=>String(s||'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));async function get(path){const r=await fetch(C.url+path,{headers:H});if(!r.ok)throw new Error('Falha ao carregar edições');return r.json()}async function boot(){try{const editions=await get('/rest/v1/editions?select=id,name,published,created_at&published=eq.true&order=created_at.desc');if(!editions.length){box.innerHTML='<div class="archive-loading">Nenhuma edição publicada ainda.</div>';return}const cards=await Promise.all(editions.map(async(e,i)=>{let cfg={};try{const rows=await get(`/rest/v1/edition_content?edition_id=eq.${encodeURIComponent(e.id)}&select=config&limit=1`);cfg=rows[0]?.config||{}}catch{}const cover=cfg.heroImage||'assets/hero.jpg',year=(cfg.siteYear||new Date(e.created_at).getFullYear()||'').toString();return `<article class="edition-card"><a class="edition-cover" href="index.html?edition=${encodeURIComponent(e.id)}" style="background-image:url('${String(cover).replace(/'/g,'%27')}')" aria-label="Abrir ${esc(e.name)}"><span>${String(editions.length-i).padStart(2,'0')}</span></a><div class="edition-copy"><h2>${esc(e.name||'Edição')}</h2><p>${esc(year)} · Edição publicada</p><div class="edition-actions"><a href="index.html?edition=${encodeURIComponent(e.id)}">LER EDIÇÃO</a><a href="revista3d-real.html?edition=${encodeURIComponent(e.id)}">REVISTA 3D</a></div></div></article>`}));box.innerHTML=cards.join('')}catch(e){box.innerHTML=`<div class="archive-loading">${esc(e.message)}. Tente novamente em instantes.</div>`}}boot()})();