(()=>{const C=window.DEUCAPA_CLOUD_CONFIG,db=C&&window.supabase?window.supabase.createClient(C.url,C.key):null,$=s=>document.querySelector(s),esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));if(!db)return;const pos=new Set(['respondeu','interessado','participante']),strong=new Set(['interessado','participante']);function pct(a,b){return b?Math.round(a/b*100):0}function group(rows,key){const m=new Map;for(const r of rows){const k=String(r[key]||'Sem informação').trim()||'Sem informação';const x=m.get(k)||{name:k,total:0,contacted:0,replied:0,interested:0,converted:0,discarded:0};x.total++;if(['contatado','respondeu','interessado','participante'].includes(r.status))x.contacted++;if(pos.has(r.status))x.replied++;if(strong.has(r.status))x.interested++;if(r.status==='participante')x.converted++;if(r.status==='descartado')x.discarded++;m.set(k,x)}return [...m.values()].filter(x=>x.total>=2).map(x=>({...x,responseRate:pct(x.replied,x.contacted),interestRate:pct(x.interested,x.contacted),conversionRate:pct(x.converted,x.contacted)})).sort((a,b)=>b.conversionRate-a.conversionRate||b.interestRate-a.interestRate||b.responseRate-a.responseRate||b.total-a.total).slice(0,6)}function ensure(){const panel=$('[data-panel="prospeccao"]');if(!panel||$('#prospectInsights'))return;const box=document.createElement('div');box.id='prospectInsights';box.className='panel-card prospect-insights';box.innerHTML='<div class="section-title"><h2>Aprendizado & conversão</h2><button id="prospectInsightsRefresh" class="text-btn" type="button">Atualizar</button></div><div id="prospectLearningState" class="prospect-stat">Calculando histórico…</div><div class="prospect-insight-grid"><div><h3>Melhores nichos</h3><div id="prospectTopCategories"></div></div><div><h3>Melhores cidades</h3><div id="prospectTopCities"></div></div></div>';panel.appendChild(box);$('#prospectInsightsRefresh').onclick=refresh}function table(items){if(!items.length)return '<p class="prospect-empty">Ainda não há amostra suficiente.</p>';return '<div class="prospect-insight-list">'+items.map(x=>`<div class="prospect-insight-row"><div><strong>${esc(x.name)}</strong><small>${x.total} históricos · ${x.contacted} contatos</small></div><div class="prospect-insight-metrics"><span>Resp. ${x.responseRate}%</span><span>Int. ${x.interestRate}%</span><span>Conv. ${x.conversionRate}%</span></div></div>`).join('')+'</div>'}async function refresh(){ensure();const state=$('#prospectLearningState');if(state)state.textContent='Atualizando aprendizado…';const {data,error}=await db.from('participant_prospects').select('id,handle,city,category,status,score,signals,created_at,updated_at').limit(1000);if(error){if(state)state.textContent='Falha ao carregar histórico.';console.error(error);return}const rows=data||[],history=rows.filter(r=>!['novo','aprovado'].includes(r.status)),learned=rows.filter(r=>Math.abs(Number(r.signals?.learning_adjustment)||0)>0),sample=history.length;if(state)state.innerHTML=sample<5?`Modo neutro · ${sample}/5 históricos necessários para iniciar o aprendizado.`:`Aprendizado ativo · ${sample} históricos · ${learned.length} candidato${learned.length===1?'':'s'} já recebeu${learned.length===1?'':'ram'} ajuste de score.`;const c=$('#prospectTopCategories'),ci=$('#prospectTopCities');if(c)c.innerHTML=table(group(history,'category'));if(ci)ci.innerHTML=table(group(history,'city'));decorate()}function decorate(){document.querySelectorAll('.prospect-card').forEach(card=>{if(card.querySelector('.prospect-learning'))return;const handle=card.querySelector('.prospect-handle')?.textContent?.replace(/^@/,'');if(!handle)return;db.from('participant_prospects').select('signals,score').eq('handle',handle).maybeSingle().then(({data})=>{if(!data)return;const a=Number(data.signals?.learning_adjustment)||0,b=Number(data.signals?.base_score);if(!a&&!Number.isFinite(b))return;const meta=card.querySelector('.prospect-signals');if(!meta)return;const s=document.createElement('span');s.className='prospect-signal prospect-learning';s.textContent=`Aprendizado ${a>0?'+':''}${a}`;s.title=Number.isFinite(b)?`Score-base ${b}; ajuste histórico ${a}; score final ${data.score}`:`Ajuste histórico ${a}`;meta.appendChild(s)})})}function boot(){ensure();refresh();new MutationObserver(()=>decorate()).observe(document.body,{childList:true,subtree:true})}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(boot,1500));else setTimeout(boot,1500)})();